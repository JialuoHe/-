1，静态链接比动态链接来得更快，极低延迟电子交易系统通常静态链接所有依赖库，以最小化运行时性能延迟


2， 正确性第一，优化第二


3， 设计针对应用用例最优的定制化数据结构是构建低延迟应用的重要一环。举例说明，对于少量元素，数组方案由于具备更好的**缓存性能（Cache performance）**反而更快；而对于大量元素，哈希表方案则更好。


4， 一开始l1,2,3都没有数据，一开始都在内存里，然后通过后续抓数据来拷贝进l1,2,3；就和l1,l2,l3一开始是空的一样，栈和堆数据一开始也不存在l1，l2,l3，都存在内存里；只是因为抓取的算法不同才导致它们的数据是否存在更靠近cpu的缓存层里。

    硬件预取器决定查到的数据存在l1，还是l2


5， 避免使用、谨慎使用或以特定的方式使用动态多态（Dynamic polymorphism）、动态内存分配（Dynamic memory allocation）和异常处理（Exception handling）。

    std::shared_ptr 是需要警惕的： 它内部使用原子操作（Atomic operations）来维护引用计数

    局部变量：在函数内创建的局部变量默认存储在栈（Stack）上，栈内存也用于存储函数返回值。假设不创建大型对象，同一范围的栈存储空间会被频繁重用，由于引用的局部性（Locality of Reference），这会带来极佳的缓存性能。

    相反，静态变量不能被其他变量重用，且访问静态变量的次数可能仅占所有内存访问的一小部分，因为static是不会随着函数结束而被释放的



6， 使用类型转换与数据转换操作
    有符号整数和无符号整数之间的转换是免费的（零成本）。将较小尺寸的整数转换为较大尺寸的整数可能需要一个时钟周期，但有时可以通过优化达到零成本。将较大尺寸的整数向下转换为较小尺寸的整数没有额外开销

    dynamic_cast的开销是昂贵的，最好不要使用dynamic

    编译器默认不同类型的指针绝对不会指向同一个地址，所以：

    double x = 100;
    const auto orig_x = x;
    // 将 double 类型的地址强制转换为无符号 64 位整数指针
    auto x_as_ui = (uint64_t *) (&x); 
    // 通过位运算，将最高位（符号位）设为 1
    *x_as_ui |= 0x8000000000000000;

    可能会出错。


7， 一般来说，对于整数和浮点值，加法最快，乘法比加法稍贵，而除法的开销则比乘法大得多。应避免在同一个表达式中混合使用单精度和双精度运算，也应避免在表达式中同时涉及浮点值和整数值。

    逻辑短路与概率排序
    逻辑与（&&）和逻辑或（||）的评估遵循“短路”原则：
    
    对于 &&：如果第一个操作数为 false，则不再评估第二个。
    
    对于 ||：如果第一个操作数为 true，则不再评估第二个。
    
    优化技巧：
    
    对于 &&：将评估为 true 的概率从低到高排列操作数。
    
    对于 ||：将评估为 true 的概率从高到低排列操作数。


    传统写法：
    if(market_state == PreOpen || market_state == Opening || market_state == Trading) {
        // 执行某些操作...
    }

    新版写法（一开始的时候，就要根据可能有的数量来规定market state的长度，并且枚举值的设定必须让每个状态对应一个独立的位，例如 PreOpen = 0x001, Opening = 0x010, Trading = 0x100）：
    if(market_state & (PreOpen | Opening | Trading)) {
        // 执行某些操作...
    }


8， 每次分支预测失败都会浪费大量的时钟周期。

    if-else 分支优化方法是保持条件数量尽可能少，并调整结构使其更具可预测性。

    for 和 while 循环如果循环次数较小，通常预测效果良好。

    switch 语句非常难以预测，一种有效的优化技巧是：将 case 标签值设为连续递增的整数，因为这样编译器极有可能将其实现为跳转表（Jump Tables），效率显著更高。



9， 只有当代码具备足够的可复用性时，才应该创建函数。创建函数的准则应该是逻辑流的清晰度和复用价值，而不是代码的长度。

    编写性能关键型函数时，如果可能，请将其放置在与其被调用的地方相同的模块（Module）中。这样做可以解锁大量的编译器优化，其中最重要的就是内联（Inlining）

    总的来说，使用 std::function 和/或 std::bind 时要非常小心，很多开发者会惊讶于这些构造在底层竟然会触发虚函数调用甚至动态内存分配

    对于原生类型（如 int, char, double），按值传递（Pass by value）效率极高。而对于作为函数参数的复合类型（如 struct 或 class），首选方式是 const 引用

    返回复合类型（如自定义的 struct 或 class）则低效得多。在某些情况下，这会导致产生多次对象拷贝。

    返回复合类型的最优方式是：让调用者先创建一个该类型的对象，然后通过引用或指针将其传递给函数，由函数直接对这个对象进行修改


10,  能不用递归就不要用递归


11， struct MarketState {
        uint8_t is_pre_open : 1; // 占用 1 位
        uint8_t is_trading  : 1; // 占用 1 位
        uint8_t padding     : 6; // 剩下的 6 位凑齐 1 字节
    };

    这样子可以提升缓存性能，但是会增加访问开销


12，    多态，继承都会遇到很多问题，但可以通过组合来替代

    struct Order { int id; double price; };

    // 继承模式：直接继承 vector 的所有能力
    class InheritanceOrderBook : public std::vector<Order> { };
    
    // 组合模式：将 vector 作为成员，更安全、更受控
    class CompositionOrderBook {
        std::vector<Order> orders_;
    public:
        auto size() const noexcept {
            return orders_.size();
        }
    };
    
    int main() {
        InheritanceOrderBook i_book;
        CompositionOrderBook c_book;
        printf("InheritanceOrderBook::size():%lu CompositionOrderBook:%lu\n",   
               i_book.size(), c_book.size());
    }

13， crtp可以通过模板写出好的多态。

14，  

        template <typename Derived> // 第一步：基类说：“我需要一个类型，我把它叫 Derived”
        class CRTPExample {
        public:
            void placeOrder() {
                // 第二步：核心魔法！
                // “虽然我现在是基类指针 (this)，但我确定我的子类类型是 Derived。”
                // “请把我的指针强制转换成子类指针，然后直接调用子类的函数。”
                static_cast<Derived*>(this)->actualPlaceOrder();
            }
            
            void actualPlaceOrder() {
                printf(“CRTPExample::actualPlaceOrder()\n”);
            }
        };
        
        
        
        // 第三步：子类继承时，把自己作为“T”传给基类
        class SpecificCRTPExample : public CRTPExample<SpecificCRTPExample> {
        public:
            void actualPlaceOrder() {
                printf(“SpecificCRTPExample::actualPlaceOrder()\n”);
            }
        };
        
        
        int main(int, char **) {
          // 1. 运行时多态：必须通过基类指针（或引用）来体现
          RuntimeExample* runtime_example = new SpecificRuntimeExample();
          runtime_example->placeOrder();
        
          // 2. 编译时多态：直接定义具体的 CRTP 对象
          CRTPExample<SpecificCRTPExample> crtp_example;
          crtp_example.placeOrder();
        
          return 0;
        }

15， 处理异常的时候建议使用 noexcept和throw，异常可以被移出hot path，所以最后一步再优化也无所谓


16，  // 1. 糟糕的对齐：变量大小交错
    struct PoorlyAlignedData {
      char c;      // 1 字节
      uint16_t u;  // 2 字节
      double d;    // 8 字节
      int16_t i;   // 2 字节
    };
    
    // 2. 优秀的对齐：从大到小排列
    struct WellAlignedData {
      double d;    // 8 字节
      uint16_t u;  // 2 字节
      int16_t i;   // 2 字节
      char c;      // 1 字节
    };

    还有一个pack，不过看起来我总感觉有点危险

17， 在处理低延迟应用时，最好完全避免动态分配内存，或者至少要非常谨慎且克制地使用。


18，  启动和停止线程需要时间，因此最好避免在需要时才启动新线程，而是使用线程池（Thread Pool）


19，  线程亲和性，比方说可以把线程绑到对应的cpu线程上，我的行情系统还没有做这个，只是单纯根据cpu数量决定了线程数量



20，  一个非常巧妙的设计，用来规避branch，也是上面提到过的

    enum class Side : int16_t { BUY = 1, SELL = -1 };
    position += int_fill_side * fill_qty;
    int last_qty[3] = {0, 0, 0}; // 准备三个槽位
    last_qty[int_fill_side + 1] = fill_qty;


21，  const size_t size = 1024;
    float x[size], a[size], b[size];
    
    for (size_t i = 0; i < size; ++i) {
        x[i] = a[i] + b[i];
    }

    for (size_t i = 0; i < size; i += 4) {
        x[i] = a[i] + b[i];
        x[i + 1] = a[i + 1] + b[i + 1];
        x[i + 2] = a[i + 2] + b[i + 2];
        x[i + 3] = a[i + 3] + b[i + 3];
    }

    ”原书示例是针对 SSE2 指令集的。SSE2 寄存器宽度：$128$ 位。单精度浮点数（float）大小：$32$ 位（$4$ 字节）。计算公式：$128 \div 32 = 4$。这意味着 SSE2 的寄存器一锅只能煮 $4$ 个 float。如果你写 += 16，编译器在底层还是得拆成 $4$ 次 SSE2 操作来完成。所以为了演示方便，作者用了 += 4。如果你使用的是更现代、更强大的 AVX-512 指令集，+= 16 才是真正的“满载运行”

22， 强度削减是一个术语，用于描述编译器的一项优化技术：将开销较大的复杂操作替换为更简单、更廉价的指令，以提升性能。一个经典的例子是：编译器将涉及“除以某个值”的操作替换为“乘以该值的倒数”。另一个例子是将“与循环索引相乘”的操作替换为“加法”操作


    #include <cstdint>

    int main() {
      const auto price = 10.125; // 价格序列如：10.125, 10.130, 10.135...
      constexpr auto min_price_increment = 0.005;
      [[maybe_unused]] int64_t int_price = 0;
    
      // 1. 未进行强度削减：执行开销巨大的除法
      int_price = price / min_price_increment;
    
      // 2. 强度削减：转换为乘法
      // 这个倒数在编译时就已经算好了
      constexpr auto inv_min_price_increment = 1 / min_price_increment;
      int_price = price * inv_min_price_increment;
    }
