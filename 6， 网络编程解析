服务端 (Server)	     客户端 (Client)       	说明
1	socket()	           socket()	        创建套接字文件描述符（fd）。
2	bind()	              -	              服务端将 fd 绑定到特定的 IP 和端口。
3	listen()	            -	              服务端进入“被动监听”状态，等待连接。
4	  -	                connect()	        客户端主动发起 TCP 三次握手。
5	accept()	            -	              服务端接受连接，返回一个新的 fd 用于通信。
6	read() / write()	   write() / read()	  连接建立后，双方开始正式交换数据。
7	close()	             close()	          数据交换完毕，关闭连接。


